id: '3'
type: epic
title: Wave 3 — Levels & warp
description: All 16 tube geometries (open + closed), color cycling, the end-of-level warp with spike collision, difficulty ramp.
priority: p1
status: backlog
repos: tempest
stories:
  - id: 3-1
    title: 16-geometry roster and tubeForLevel selector
    points: 3
    priority: p1
    status: done
    assigned_to: slabgorb
    started: '2026-06-25'
    repos: tempest
    workflow: tdd
    completed: '2026-06-25'
    review_verdict: approved
  - id: 3-2
    title: End-of-level warp transition with geometry switch
    description: 'End-of-level warp transition that SWAPS THE GEOMETRY (Wave 3 plan Task 2 -> advanceLevel). CARRYOVER FROM STORY 3-1 REVIEW: today startLevel/checkLevelClear (src/core/sim.ts) increment s.level but never call tubeForLevel(s.level) or resize s.spikes, so the 16-geometry roster built in 3-1 is only used at level 1. This story MUST wire the per-level geometry swap + spike-array resize, or the entire Wave 3 roster stays invisible past level 1. Ref: docs/superpowers/plans/2026-06-25-tempest-wave-3-levels-warp.md Task 2; sprint/archive/3-1-session.md Delivery Findings (Reviewer).'
    points: 3
    priority: p1
    status: done
    assigned_to: slabgorb
    started: '2026-06-25'
    repos: tempest
    workflow: tdd
    acceptance_criteria:
      - 'advanceLevel(s) swaps geometry: s.tube = tubeForLevel(s.level) so each level actually shows its roster shape (NOT the level-1 circle).'
      - 'advanceLevel resizes the spike array to the new geometry: s.spikes = new Array(s.tube.laneCount).fill(0) — laneCount varies (12/14/15/16) across the roster, so a stale-length array is a bug.'
      - 'advanceLevel wraps the player lane into the new tube: s.player.lane = wrapLane(s.tube, s.player.lane), keeping the Claw in range when laneCount shrinks.'
      - Clearing a level enters 'warp' mode (warp.progress 0->1 at WARP_SPEED) instead of advancing immediately; advanceLevel runs on warp completion and resets warp.progress to 0, mode to 'playing'.
      - 'Regression guard the 3-1 suite could not cover: a test asserts that after a level transition out.tube.laneCount === tubeForLevel(out.level).laneCount AND out.spikes.length === tubeForLevel(out.level).laneCount.'
    completed: '2026-06-25'
    review_verdict: approved
  - id: 3-3
    title: Spikes crash the Claw during the warp
    points: 2
    priority: p1
    status: done
    started: '2026-06-25'
    repos: tempest
    workflow: tdd
    completed: '2026-06-25'
    review_verdict: approved
  - id: 3-4
    title: Difficulty ramp continues past the geometry cycle
    points: 2
    priority: p1
    status: backlog
    repos: tempest
    workflow: tdd
  - id: 3-5
    title: Per-level color cycling, open-tube and warp rendering
    points: 2
    priority: p1
    status: backlog
    repos: tempest
    workflow: trivial
