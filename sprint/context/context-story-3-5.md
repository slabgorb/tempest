# Story 3-5 Context

## Title
Per-level color cycling, open-tube and warp rendering

## Metadata
- **Story ID:** 3-5
- **Type:** story
- **Points:** 2
- **Priority:** p1
- **Workflow:** trivial
- **Repo:** tempest
- **Epic:** Wave 3 — Levels & warp

## Problem
REMAINING SCOPE: warp rendering only. Per-level color cycling and open-tube rendering already shipped in PR #10 (feat/standalone-polish, merged 2026-06-25) via the shell-polish port — see src/shell/render.ts (LEVEL_COLORS cycling + boundary-safe open-tube spokes/rings). This story now covers the third deliverable from its title: a distinct visual for the end-of-level warp (mode === 'warp', warp.progress 0->1). Shell-only; core already exposes warp state. Ref: src/core/state.ts WarpState, src/core/sim.ts stepWarp/advanceLevel; warp collision landed in 3-3.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- While mode === 'warp', the renderer shows a distinct warp transition driven by warp.progress (NOT just the static playing scene).
- The Claw visibly travels down its lane toward the vanishing point as warp.progress goes 0->1, making the 3-3 spike crash legible.
- Color cycling and open-tube rendering (delivered in PR #10) still work — no visual regression on normal play.
- Shell-only: core/ untouched (no DOM/time/random added to core); tsc --noEmit and the full test suite stay green.

---
_Generated by `pf context create story 3-5` from the sprint YAML._
